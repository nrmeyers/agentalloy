<!--
VERSION BUMP IS AUTOMATIC — do NOT hand-edit the version in pyproject.toml / uv.lock.
The tier is derived from THIS PR's title (Conventional Commit type):
    feat!  (or a BREAKING CHANGE marker)  → major
    feat                                  → minor
    fix / perf / anything else touching shipped code → patch
Docs / CI / test / tooling-only PRs don't version at all.
CI writes pyproject.toml + uv.lock on the branch; merging cuts the release.
Get the title's type right — it's the only lever. Full rule: RELEASE.md §4.
-->

## Context

<!-- Why this change exists: the problem or root cause it addresses. -->

## Change

<!-- What the fix/feature does. -->

## Tests

<!-- How it was verified: commands run + results. -->
