PLAN PHASE for AgentAlloy v2.0 — Milestone 1 (local agent). DESIGN is approved; the approach is authoritative: state store, phase=design, slug=v2-local-agent-mvp, name=approach.artifact (~35k chars — READ IT FIRST, esp. §24 build order, §0 OverGraph-as-Rust-core, §9 ColBERT risk, §12 DuckDB one-RW/many-RO, §23 risks). The full product spec: phase=spec, name=spec.artifact (~27.6k).

PLAN deliverables (record via the state panel): (A) tasks.artifact = the dependency DAG from design §24 decomposed into per-task build contracts, and (B) test-plan.artifact = the test strategy. Plan produces tasks + contracts + test plan ONLY — no src/ code.

TASKS to emit (one build contract per task, domain_tags ≤2, touches/avoids/success_criteria each; the build_contracts_cover_tasks gate requires contracts ≥ tasks). Order per design §24: T1 scaffold (repo layout, pyproject, justfile, mise.toml+rust, rust/ workspace, src/newagent/, config.py, 'just ci' green on skeleton) · T2 env prereqs + ColBERT --pooling none spike (install Rust via mise, obtain LFM350M GGUFs, start embed/rerank servers on :48951/:48952, decide §9 primary vs Fallback-B, runbook/ deliverable) · T3 newagent-core crate (overgraph code index build+hybrid search, validate.rs, cargo tests) · T4 state-action reliability validation GATE (~30-40 structured state-op prompts, temp 0, strict json_schema, pass ≥90% action+args full-correct + zero schema-invalid; MUST pass before T8) · T5 interpreter (client.py, protocol.py read+state, interpreter.py LangGraph sub-graph named exits, executors.py, validate.py, 'newagent ask' read path) · T6 skill corpus+engine (skill_corpus Rust, skill_engine.py retrieve-then-infer) · T7 checkpointer+stores (checkpointer.py BaseCheckpointSaver over duckdb-python, state_store.py, telemetry.py, Rust state_read/telemetry_read RO) · T8 phase machine (phase_machine.py LangGraph outer graph, interrupt() approval gates, Send fan-out, approval+digest §13, sessions/cursor §16) · T9 steering proxy + REST/CLI (steering/, server.py, cli.py) · T10 integration + AC verification (full main-lane run via real harness, stash->kill->resume, one build contract per task, --force-never-bypasses, v2-never-writes-source test) · T11 M1.x ColBERT MaxSim (only if T2 spike triggered Fallback).

TEST PLAN: offline-green (AC-12: FakeLM/FakeEmbed/FakeRerank/MockHarness/temp stores, no live model/service/harness); per-AC verification mapping (AC-1..AC-16 to tasks); the state-action reliability harness (T4) is reusable for the M3 in-CI gate; a Rust-RO-only-enforcement test (AC: DuckDB single-RW); the v2-never-writes-source test (AC-16); the full-suite gate 'just ci' (ruff+clippy+mypy --strict+pytest+cargo test).

HARD CONSTRAINTS (do not re-litigate; locked spec tenets T1-T8 + design): native LFM stack (LFM2.5-2.6B driver :50001 temp 0 strict json_schema + reasoning-on thinking budget; LFM2.5-Embedding-350M embed port; LFM2.5-ColBERT-350M MaxSim rerank port; GGUFs not yet on disk); dev ports :48950/:48951/:48952 with final config flip to v1's :47950/:47951/:47952 (drop-in end state); storage OverGraph (code index + skill corpus) + DuckDB (state + telemetry; Python owns the single RW handle, Rust opens RO); one newagent-core Rust crate on the overgraph=0.17 crate + duckdb RO + tantivy + tree-sitter, PyO3 one-way (no Rust->Python callbacks); LangGraph checkpointer IS the state store; full main lane only (sdd-fast/add-skill = M3); minimal corpus at M1; minimal steering proxy at M1 (composition auto-injection = M2); harness executes code, v2 never writes target-repo source; knowledge_* actions parked (structured empty at M1); v1 at ~/dev/agentalloy is CONCEPTS-only, never modified, never imported/called by v2; T4 reliability gate must pass before T8.