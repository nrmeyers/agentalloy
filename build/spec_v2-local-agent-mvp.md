Build a runnable end-to-end MVP scaffold of AgentAlloy v2.0 (local-agent mode): a fully-local, zero-cloud-token, read-only agent in Python + Rust. A question goes in; the local LFM2.5-2.6B model (live at 127.0.0.1:50001) classifies it into one of 10 read-only actions (code_search, symbols, knowledge_why, knowledge_related, knowledge_entities, artifact_body, contract_detail, telemetry, get_skill_for, none), fills arguments under the action JSON schema, executes, loops/stops, and generates an answer. The scaffold must take a REAL question through the LIVE model and return an answer + step trace. Ships with a tests skeleton (injected fake client, green without a model). Rust is confirmed in the MVP. Spec must resolve: (1) where the scaffold lives (standalone in newagent vs module-in-v1), (2) how the 10 executors run (live v1 HTTP :47950 vs in-process vs stubs), (3) the Python/Rust boundary and interop mechanism, (4) stack and tooling (pyproject/uv/ruff/mypy-strict/pytest + cargo/clippy), (5) model-wiring config shape and step budget. Reference: spike in newagent/ (protocol.py, client.py, validate.py, run.py, score.py, tasks/tasks.jsonl, results/), v1 feature/local-agent branch commit 761b985 (src/agentalloy/local_agent/*, 364 tests, ruff+mypy-strict clean), portable patch at /home/nmeyers/dev/0001-feat-local-agent-opt-in-local-agent-mode-v2-2.6B-mod.patch, design doc docs/local-agent-design.md on that branch. Live endpoints verified at intake: :50001 (model) and :47950 (v1 service).