DESIGN PHASE for AgentAlloy v2.0 (product spec approved 2026-09-03; full spec: state store, phase=spec, slug=v2-local-agent-mvp, name=spec.artifact, ~27.6k chars — READ IT FIRST; it is authoritative).

DESIGN must answer the spec's 'Open design questions > M1' section: (1) interpreter tool surface (final action list + JSON schemas: read seed from the spike's 10-way in newagent/protocol.py + state/contract actions; get_skill_for vs per-turn composition; state-action safety prompts); (2) skill inference mechanism (retrieve-then-infer: OverGraph candidate retrieval -> LFM selection/composition; budgets; rendering into the instruction set; dedupe); (3) code index design (tree-sitter grammars, OverGraph usage, hybrid dense+BM25, ColBERT second-stage integration); (4) ColBERT serving protocol (per-token vectors from a served ColBERT model — llama.cpp /embeddings pools by default; MaxSim math locked to the Rust index); (5) embedding API shape against the embed port (batching, MRL 768 slice at index-build); (6) LangGraph graph structure (outer phase machine: nodes=phases, gate-passing edges, interrupt() at approvals, Send-based task fan-out; interpreter sub-graph; state schemas); (7) LangGraph checkpointer on DuckDB (custom BaseCheckpointSaver over duckdb-python; checkpoint tables vs human-facing state/contracts/artifacts tables; Rust read via duckydb); (8) approval + digest mechanics (digest location, void-on-edit enforcement, single-call advance-with-approval); (9) steering surface shape (the minimal steering proxy: /proj/<id>/v1 pass-through + state-panel/phase-context injection on dev port :48950; composed-instruction/persona delivery; harness ingestion); (10) telemetry schema (interpreter trace fields, phase durations, approval waits, task fan-out); (11) session/cursor mechanics (work-item cursor across stash/resume; archive/cancel terminal states); (12) M1 corpus (which packs; OverGraph layout + edge generation; v1's generate-skill-edges.py is the precedent); (13) config shape (env-driven: ports, step budget, result cap, driver max_tokens/thinking budget); (14) REST/CLI surface inventory (ask, state panel, advance/approve, sessions, contracts, artifacts, compose); (15) AC-8 budget semantics per interpreter invocation type; (16) contract schema with domain_tags required (<=2). Plus the LOCKED first build task: interpreter state-action reliability validation (structured state calls at temp 0, validation + reject-on-error) — design its task set + pass criteria.

HARD CONSTRAINTS (do not re-litigate; locked in spec tenets T1-T8): tenets as recorded; native LFM stack (LFM2.5-2.6B driver :50001 temp 0 strict json_schema + reasoning-on budgeting; LFM2.5-Embedding-350M embed port; LFM2.5-ColBERT-350M MaxSim rerank port; GGUFs not yet on disk — runbook is a deliverable); dev ports :48950/:48951/:48952 with final config flip to v1's ports :47950/:47951/:47952 (drop-in end state); storage OverGraph (code index + skill corpus) + DuckDB (state + telemetry, Python writes / Rust reads via duckydb); LangGraph checkpointer IS the state store; one-way PyO3 boundary (Rust data layer, Python workflow/request layer, no Rust->Python callbacks); full main lane only (sdd-fast/add-skill = M3); minimal corpus at M1; minimal steering proxy at M1 (composition auto-injection = M2); harness executes code, v2 never writes target-repo source; knowledge_* actions parked (structured empty at M1, follow-up spec item); v1 at ~/dev/agentalloy is a CONCEPTS-only reference, never modified, never imported/called by v2 (v1 orchestrates v2's own build as an external tool); offline-green tests (injectable LM/embed/rerank clients, mock harness); one-command build; ruff + clippy + mypy --strict + pytest + cargo test all green.