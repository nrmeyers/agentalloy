# Skill Review History (placeholder)

> **STATUS — under redesign.** This directory is reserved for per-skill QA
> reports produced by the authoring/QA pipeline. That pipeline is being
> redesigned and does not yet emit reports here, so the directory currently
> holds only a `.gitkeep`. The report format and generation flow will be
> defined when the redesigned pipeline ships.

For the authoring pipeline's target design and the override system, see
[skill-authoring-and-overrides-spec.md](../skill-authoring-and-overrides-spec.md).

The authoritative R1-R8 quality contract lives at
`src/agentalloy/_packs/meta/sys-skill-authoring-rules.md`. Do not treat any
older R1-R8 list as current — defer to the meta file.
