# Meta-Skill Corpus Delivery — Task Plan

Each task names the AC it satisfies (spec) and the DK it implements.

## T1 — Convert `sys-skill-authoring-rules.md` → `sdd/sys-skill-authoring-rules.yaml`  *(AC 1, 3; DK2, DK3, DK4)*

New file `src/agentalloy/_packs/sdd/sys-skill-authoring-rules.yaml`:

- `skill_id: sys-skill-authoring-rules`, `canonical_name` from the `.md`'s H1.
- `category: tooling` (unchanged from the `.md`).
- `skill_class: system`.
- `always_apply: false`, `phase_scope: [add-skill]`, `category_scope: null`. (DK3)
- `author`, `change_summary` carried from the `.md` header, with a dated addendum:
  "converted from `_packs/meta/sys-skill-authoring-rules.md` to a phase-scoped
  system skill — see `meta-skill-corpus-delivery.design/approach.md` DK4."
- `raw_prose:` the `.md`'s full body verbatim, after its header block. No
  `fragments:` key (DK4 — system-class auto-generates one guardrail fragment).
- **Do not edit the prose content** — this is a delivery-mechanism fix, not a
  content revision. Byte-for-byte body carryover, diffable against the source.

Leave `_packs/meta/sys-skill-authoring-rules.md` in place (untouched, unreferenced
by any pack — it was never reachable anyway; deleting it is a separate cleanup
call, not required for delivery, and risks losing the source-of-truth if the YAML
conversion has a transcription bug).

## T2 — Convert `sys-skill-review-verdict.md` → `sdd/sys-skill-review-verdict.yaml`  *(AC 1, 3; DK2, DK3, DK4, DK6)*

Same procedure as T1, source file
`src/agentalloy/_packs/meta/sys-skill-review-verdict.md` (authored this session,
slice 2 of install-pack-semantic-gate). Per DK6, no prose changes — carry verbatim.

## T3 — Register both in `sdd/pack.yaml`; bump version  *(AC 1; DK2)*

Add both to `sdd/pack.yaml`'s `skills:` list (`{skill_id, file, fragment_count}` —
`fragment_count: 0`, verified against `sys/pack.yaml`'s real system-class entries,
e.g. `sys-ci`, which all record `0` — the manifest's `fragment_count` tracks
authored `fragments:` entries, and system-class skills have none; the one
guardrail fragment is generated by the importer, not authored). Bump `sdd/pack.yaml`
version (was 1.6.0 after slice 2 of the gate work → 1.7.0).

## T4 — Wire `requires:` edges in `sdd-add-skill.yaml`  *(AC 3; DK5)*

Add `requires: [sys-skill-authoring-rules, sys-skill-review-verdict]` to
`sdd-add-skill.yaml`'s header. Prose already references both by name (from this
session and the prior one) — leave prose as-is, this is additive metadata only.

## T5 — Delivery-proof test  *(AC 1, 2, 3 — the acceptance test)*

`tests/install/test_meta_skill_delivery.py`: import the two new YAMLs through the
real ingest path (`install/importer.py::import_skill` or the higher-level pack
import used by existing pack tests — match whatever `tests/install/test_seed_corpus.py`
or a sibling uses for a from-scratch DB), then call
`retrieval.system.retrieve_system_fragments(store, phase="add-skill", category=None)`
and assert both skill_ids appear in `applied_skill_ids` and their fragments are in
`candidates`. Also assert **absence** under a different phase (e.g. `phase="build"`)
— proves the phase-scoping actually restricts delivery, not just that it's
possible under one phase. This is the AC-1 "delivery is proven, not asserted" test
the spec requires.

Regression guard (AC 2): assert the 5 pre-existing `sys/` pack skills' applicability
is unaffected — this task doesn't touch `retrieval/**`, so a full existing-suite
green run is the practical proof; no new test needed beyond running it.

## Out of this task (do not do here)

- Converting the other 7 meta/conventions skills — no confirmed consumer (see
  Scope boundary).
- Deleting the source `.md` files from `_packs/meta/`.
- Touching `RETRIEVAL_GRAPH_EXPAND`, `retrieval/domain.py`, or any graph-expand
  code (Option B — not chosen).
- Flipping `AGENTALLOY_INSTALL_REQUIRE_REVIEW` (unrelated feature).
