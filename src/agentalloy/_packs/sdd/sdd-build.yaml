skill_id: sdd-build
canonical_name: SDD — Build (sdd-build)
description: "Execute the design's task plan as code: work the ordered slices in docs/design/<slug>/tasks.md one at a time, test-first, each proven green against the spec's acceptance before moving on."
category: operational
skill_class: workflow
domain_tags:
  - spec-driven-development
  - implementation
  - coding
  - build
requires: []
always_apply: false
applies_to_phases:
  - build
exit_gates:
  all_of:
    - artifact_exists:
        path: "src/**"
    - artifact_exists:
        path: "tests/**/*.py"
signal_keywords:
  - done building
  - ready for qa
  - implementation complete
  - next phase
contract_template: |
  ---
  phase: build
  task_slug: {{task_slug}}
  route: full
  domain_tags: []
  scope:
    touches: []
    avoids: []
  success_criteria: []
  related_contracts: []
  created_at: {{created_at}}
  ---

  # {{task_slug}}

  ## Scope in a sentence

  <what this changes, and the boundary of "this">

  ## Plan

  Approach + task order live in docs/design/{{task_slug}}/ (approach.md, tasks.md).
# Workflow skills are selected by applies_to_phases (signal layer), not by
# phase_scope, so this is left empty and scoped via category instead.
phase_scope: []
category_scope: [process]
author: navistone
change_summary: "rewrite as a tight, contract-driven build (work the design folder's tasks.md top to bottom, test-first, scope-honest); drop the overview filler, the standalone PostToolUse section (folded to one line), and the runtime-injected eval block"
raw_prose: |
  # SDD — Build

  Spec said *what*, design said *how* and *in what order*. Build is execution:
  work the design's task slices, one at a time, each proven by a test before you
  move on. No new decisions here — if you're inventing architecture or
  acceptance, you're in the wrong phase.

  ## 1. Load the plan

  Read `docs/design/<slug>/tasks.md` (your worklist) and `approach.md` (the how
  + decisions you're implementing against), plus `docs/spec/<slug>.md` (the
  acceptance you're building toward). Take the tasks in order; pull in the
  folder's component files as the task you're on references them.

  ## 2. Open a build contract

      agentalloy contract init --phase build --slug <task-slug>

  Fill `domain_tags` and `scope.touches/avoids`. Writing it triggers the hook to
  inject the domain skills matching your tags on the next turn — the help
  arrives automatically. (If it doesn't: `agentalloy compose --contract
  .agentalloy/contracts/build/<slug>.md`.)

  ## 3. Test first, one concern at a time

  For each slice: write the test that encodes its spec acceptance, watch it fail,
  then write the code that makes it pass. Keep changes small and committed — one
  concern per commit, a green suite at every step. A slice isn't done until its
  test is green and the rest of the suite still is.

  ## 4. Stay inside the scope you declared

  Work within the contract's `scope.touches`. Spreading wider than you planned is
  the signal to stop and check: is this scope creep (cut it), or did design miss
  something (fix it there — `agentalloy phase set design`)? Don't quietly grow
  the blast radius.

  ## 5. Done when

  Every spec acceptance criterion has a passing test, the full suite is green
  with no regressions, and the work lives under `src/` with tests under
  `tests/`. Then hand to **qa**.

  ## Not this

  Don't skip the test because the code "obviously works" — the test is the
  acceptance, made executable. Don't gold-plate beyond the spec, and don't fold
  in unrelated fixes you spot along the way (note them, leave them). If the
  design turns out wrong mid-build, don't freelance a new one — bail to `design`
  and re-plan.
